[project]
name = "expertforge"
version = "0.1.0"
description = "Configurable multi-domain AI expert agent CLI with strategy routing"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.0",
    "pyyaml>=6.0.3",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
