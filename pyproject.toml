[project]
name = "expertforge"
version = "0.1.0"
description = "Single-domain configurable AI expert agent CLI"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.0",
    "pyyaml>=6.0.3",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
